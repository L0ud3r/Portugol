
fun factorial(min, max)
    res <- min;
    para i de min+1 ate max faca
        res <- res * i;
    fimpara;
    res;
endfun;

fun factorial(x)
    factorial(1, x);
endfun ;

a <- 1;
read a;
f <- factorial(a);
say f





