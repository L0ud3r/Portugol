
fun factorial(min, max)
    res <- min;
    for i [min+1 ... max]
        res <- res * i;
    endfor;
    res;
endfun;

fun factorial(x)
    factorial(1, x);
endfun ;

a <- 1;
read a;
f <- factorial(a);
say f





